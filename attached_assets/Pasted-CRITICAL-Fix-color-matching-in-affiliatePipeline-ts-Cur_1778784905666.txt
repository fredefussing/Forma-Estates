CRITICAL: Fix color matching in affiliatePipeline.ts. Current system matches wrong colors (blue tables in white rooms). Three fixes required.

FIX 1 — Update vision prompt in affiliatePipeline.ts:

Replace the vision prompt with this exact version:

const VISION_PROMPT = `Analyze this room photo with EXTREME color precision.

Return JSON:
{
  "description": "short description",
  "types": ["sofa", "coffee_table", "armchair"],
  "colors": ["beige", "natural_wood", "green"],
  "styles": ["scandinavian"],
  "materials": ["fabric", "wood", "ceramic"]
}

COLOR RULES — use ONLY these exact values:
sofas/chairs: beige, grey, white, black, brown, cream, tan, olive
wood: natural_wood, light_wood, dark_wood, oak, walnut, pine
metals: brass, gold, chrome, black_metal
plants: green
glass: clear_glass, tinted_glass

Be SPECIFIC. "beige fabric sofa" not "light colored seating".`;

Also change detail from "low" to "high":
image_url: { url: ..., detail: "high" }

FIX 2 — Update scoring in affiliatePipeline.ts:

Change the scoring to this:

// STEP 1: MANDATORY type filter — only these types
WHERE p.tags->>'type' = ANY(${vision.types})

// STEP 2: Score — COLOR is most important (60%), style (20%), material (20%)
const scoreExpr = sql<number>`
  (CASE WHEN ${products.tags}->>'color' = ANY(${vision.colors}) THEN 60 ELSE 0 END) +
  (CASE WHEN ${products.tags}->>'style' = ANY(${vision.styles}) THEN 20 ELSE 0 END) +
  (CASE WHEN ${products.tags}->>'material' = ANY(${vision.materials}) THEN 20 ELSE 0 END)
`;

ORDER BY score DESC, random()
LIMIT 12

FIX 3 — Add multi-object matching:

Update the vision response to include individual objects:

// In vision response parsing:
const vision = JSON.parse(response.choices[0].message.content);

// Extract objects array if present, fallback to single object
const objects = vision.objects || [{
  type: vision.types?.[0],
  color: vision.colors?.[0],
  style: vision.styles?.[0],
  material: vision.materials?.[0]
}];

// Match each object individually, max 2 matches per object
const allMatches = [];
for (const obj of objects.slice(0, 3)) {  // max 3 objects
  const matches = await matchSingleObject(designId, obj);
  allMatches.push(...matches);
}

// Deduplicate by product_id
const seen = new Set();
const unique = allMatches.filter(m => {
  if (seen.has(m.id)) return false;
  seen.add(m.id);
  return true;
}).slice(0, 6);  // max 6 total

FILES TO MODIFY:
- server/affiliatePipeline.ts (all 3 fixes)

NO OTHER CHANGES. Test with a new design generation after implementing.