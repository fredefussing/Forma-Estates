import { useState } from 'react';
import { ShopThisStyle } from './ShopThisStyle';

interface Props {
  aiImageUrl: string;
  roomType: string;
  style: string;
  budget: 'budget' | 'standard' | 'luxury';
}

export function AIResultWithShopping({ 
  aiImageUrl, 
  roomType, 
  style,
  budget 
}: Props) {
  const [showProducts, setShowProducts] = useState(false);
  const [activeTab, setActiveTab] = useState<'look' | 'products'>('look');

  return (
    <div className="space-y-6">
      {/* AI Billede */}
      <div className="relative rounded-xl overflow-hidden shadow-lg">
        <img 
          src={aiImageUrl} 
          alt="Dit AI design" 
          className="w-full h-auto"
        />

        {/* Overlay info */}
        <div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent p-6">
          <div className="flex items-end justify-between">
            <div>
              <p className="text-white/80 text-sm">Dit nye {getRoomLabel(roomType)}</p>
              <h2 className="text-white text-2xl font-bold">
                {style.charAt(0).toUpperCase() + style.slice(1)} stil
              </h2>
            </div>

            {/* Toggle */}
            <div className="flex bg-white/20 backdrop-blur-sm rounded-lg p-1">
              <button
                onClick={() => setActiveTab('look')}
                className={`px-4 py-2 rounded-md text-sm font-medium transition ${
                  activeTab === 'look' 
                    ? 'bg-white text-gray-900' 
                    : 'text-white hover:bg-white/10'
                }`}
              >
                Se billede
              </button>
              <button
                onClick={() => setActiveTab('products')}
                className={`px-4 py-2 rounded-md text-sm font-medium transition ${
                  activeTab === 'products' 
                    ? 'bg-white text-gray-900' 
                    : 'text-white hover:bg-white/10'
                }`}
              >
                Shop looket
              </button>
            </div>
          </div>
        </div>
      </div>

      {/* Info bar */}
      <div className="flex items-center gap-4 text-sm text-gray-500 bg-gray-50 p-4 rounded-lg">
        <div className="flex items-center gap-2">
          <svg className="w-5 h-5 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <span>Dette er et AI-genereret inspirationsbillede</span>
        </div>
        <div className="h-4 w-px bg-gray-300" />
        <span>Produkterne nedenfor matcher stilen og budgettet</span>
      </div>

      {/* Shop sektion */}
      <ShopThisStyle 
        style={style}
        roomType={roomType}
        budget={budget}
        aiImageUrl={aiImageUrl}
      />

      {/* CTA til præcis tilbud */}
      <div className="bg-blue-50 border border-blue-200 rounded-xl p-6 text-center">
        <h3 className="text-lg font-semibold text-blue-900">
          Vil du have præcis dette look i dit hjem?
        </h3>
        <p className="text-blue-700 mt-2">
          Vi finder de rigtige produkter og sætter dig i kontakt med en håndværker
        </p>
        <button className="mt-4 bg-blue-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-blue-700 transition">
          Få 3 tilbud — gratis
        </button>
      </div>
    </div>
  );
}

function getRoomLabel(roomType: string): string {
  const labels: Record<string, string> = {
    living_room: 'stue',
    bedroom: 'soveværelse',
    kitchen: 'køkken',
    bathroom: 'badeværelse',
    dining_room: 'spisestue',
    office: 'kontor',
    hallway: 'entré'
  };
  return labels[roomType] || roomType;
}