OVERORDNET LAYOUT
min-h-screen bg-background
└── <header> sticky top-0 z-50 h-16   ← navigation
└── <main> max-w-5xl mx-auto px-6    ← indhold
    ├── Step-indikator (vises fra step 2+)
    └── AnimatePresence (fade + slide, 0.3s easeOut)
        ├── Step 1 — Upload
        ├── Step 2 — Konfigurer
        └── Step 3 — Resultat

STEP-INDIKATOR
3 cirkler (w-7 h-7, rounded-full, text-[11px])
  Aktiv/gennemført:  bg-foreground text-background
  Inaktiv:           bg-muted text-muted-foreground
  Gennemført:        viser <Check w-3 h-3> i stedet for tal
Linje imellem:  w-10 (mobil) / w-16 (desktop), h-px
Labels:  text-xs, hidden sm:inline
  → "Upload billede" / "Vælg stil & budget" / "Resultat"

STEP 1 — UPLOAD
max-w-xl mx-auto pt-16/24 pb-16 text-center
H1:   text-4xl sm:text-[3.25rem] font-semibold tracking-tight
      leading-[1.1] mb-5
      → "Se dit nye rum på under 60 sekunder med AI"
Brødtekst: text-[15px] text-muted-foreground leading-relaxed max-w-sm
To checkmarks:  text-[13px] font-semibold / text-[13px] text-muted-foreground
Upload-boks:
  rounded-2xl border border-border/70 bg-card/40
  hover: border-foreground/20 bg-card/70 shadow-lg
  Indre padding: py-16/20 px-8
  Ikon: w-12 h-12 rounded-xl bg-foreground/[0.04]
  → Upload ikon: w-5 h-5 text-foreground/50
  Tekst: text-[15px] font-medium + text-sm muted + text-xs muted/60
Nedre guide (1→2→3):
  Numre: w-5 h-5 rounded-full border text-[10px]
  Labels: text-xs hidden sm:inline
  Pile: ArrowRight w-3 h-3

STEP 2 — KONFIGURER
Grid: grid-cols-1 lg:grid-cols-12 gap-8
VENSTRE (col-span-5):  ← Dit billede
  Label: text-xs tracking-widest uppercase muted font-medium
  "Skift"-knap: ghost sm, text-xs h-7 px-2
  Billede: rounded-xl border/60 max-h-[420px] object-contain
HØJRE (col-span-7):   ← Indstillinger
  Sektioner adskilt af <Separator bg-border/40>
  ① RUMTYPE
     Label: text-xs tracking-widest uppercase muted font-medium mb-4
     Grid: grid-cols-2 sm:grid-cols-3 gap-2
     Knapper:
       Størrelse: px-3.5 py-2.5 rounded-lg border text-sm
       Valgt:     border-foreground bg-foreground text-background
       Normal:    border-border/60 text-foreground/70
       hover:     border-foreground/30 text-foreground
       Ikon: w-4 h-4 opacity-70
       Tekst: text-[13px] font-medium truncate
     15 rumtyper vises alle
  ② STIL
     Label: identisk med rumtype-label
     Grid: grid-cols-2 gap-2
     Knapper: px-3.5 py-3 rounded-lg, flex-col layout
       Titel: text-sm font-medium
       Beskrivelse: text-xs mt-0.5 muted
       "GRATIS" badge: text-[10px] font-medium emerald-600
       Lås-ikon: Lock w-3.5 h-3.5 (betalte stilarter)
       Låst tilstand: border-border/40 bg-muted/30 cursor-not-allowed
     8 stilarter vises
  ③ BUDGET (kun synlig når stil er valgt — fade-in animation)
     <BudgetSlider> komponent:
       Label: text-xs tracking-widest uppercase muted
       Beløb: text-2xl font-semibold tabular-nums tracking-tight
       Tier-badge: px-2.5 py-1 rounded-full text-[11px] font-medium
         Budget  → grøn prik (emerald-500)
         Standard → sort prik (foreground)
         Luksus   → gylden prik (amber-500)
       Slider: Shadcn Slider, min=0 max=200 step=1
         Logaritmisk skala: 0-100 → 0-100.000 kr
                            100-200 → 100.000-1.000.000 kr
       Tier-info boks: rounded-lg border/40 bg-card/50 p-4
         Beskrivelse: text-sm text-foreground/80
         Retailer-badges: px-2.5 py-0.5 rounded-full text-[11px]
                          bg-foreground/5 border-border/30
  ④ INKLUDER PLANTER
     px-4 py-3 rounded-lg border/60 bg-card/40
     Emoji: text-lg 🌿
     Titel: text-sm font-medium
     Subtekst: text-xs muted
     Toggle: h-6 w-11 rounded-full
       Aktiv: bg-foreground
       Inaktiv: bg-muted
  ⑤ CREDITS INFO
     text-xs muted-foreground
     Admin: "∞ billeder (admin)"
     Bruger: "X billeder tilbage"
  ⑥ GENERER-KNAP
     w-full h-12 text-sm font-medium tracking-wide
     Label: "Se dit rums potentiale →"
     Ikon: TrendingUp w-4 h-4 (eller Loader2 under generering)

DATA-FLOW (step 2 → 3)
State i home.tsx:
  selectedFile  → File-objekt
  previewUrl    → ObjectURL til preview
  roomType      → RoomType string
  style         → DesignStyle string
  budget        → number (kr)
  tier          → BudgetTier (beregnet fra budget)
  includePlants → boolean
Ved klik "Generer":
  FormData: { image, roomType, style, budget, includePlants }
  POST /api/designs
  → Server slår op: getRoomStylePrompt(style, roomType, tier)
  → Collov edit/generate
  → Poll hvert 2s (maks 90s)
  → Rå CDN URL gemt i DB
  → Frontend poller design status og skifter til step 3