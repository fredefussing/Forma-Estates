// /src/components/HeroSection.tsx

export function HeroSection() {
  return (
    <div className="hero" style={{
      height: '100vh',
      position: 'relative',
      overflow: 'hidden',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }}>
      {/* Video baggrund i stedet for statisk billede */}
      <video 
        autoPlay 
        muted 
        loop 
        playsInline
        style={{
          position: 'absolute',
          top: 0,
          left: 0,
          width: '100%',
          height: '100%',
          objectFit: 'cover',
          zIndex: -2
        }}
      >
        <source src="/videos/room-transformation.mp4" type="video/mp4" />
      </video>
      
      {/* Mørk overlay så tekst er læsbar */}
      <div style={{
        position: 'absolute',
        top: 0,
        left: 0,
        width: '100%',
        height: '100%',
        background: 'rgba(0,0,0,0.4)',
        zIndex: -1
      }} />
      
      {/* Indhold */}
      <div style={{ textAlign: 'center', color: 'white', maxWidth: '800px' }}>
        <h1 style={{
          fontFamily: '"Playfair Display", serif',
          fontSize: '4rem',
          fontWeight: 400,
          marginBottom: '1rem',
          lineHeight: 1.1
        }}>
          Transformer dit hjem<br />med AI
        </h1>
        
        <p style={{
          fontSize: '1.25rem',
          opacity: 0.9,
          marginBottom: '2rem'
        }}>
          Se dit rum redesignet på sekunder. Fra idé til virkelighed.
        </p>
        
        <button style={{
          background: 'white',
          color: 'black',
          padding: '1rem 2.5rem',
          fontSize: '1.1rem',
          border: 'none',
          borderRadius: '50px',
          cursor: 'pointer',
          fontWeight: 500
        }}>
          Start dit design →
        </button>
      </div>
    </div>
  );
}
2. Upload sektion der føles premium
tsx
Copy
// /src/components/UploadSection.tsx

export function UploadSection() {
  const [isDragging, setIsDragging] = useState(false);
  
  return (
    <div style={{
      minHeight: '100vh',
      background: 'linear-gradient(135deg, #f5f5f5 0%, #e8e8e8 100%)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '2rem'
    }}>
      <div style={{
        background: 'white',
        borderRadius: '24px',
        padding: '3rem',
        maxWidth: '600px',
        width: '100%',
        boxShadow: '0 20px 60px rgba(0,0,0,0.1)'
      }}>
        <h2 style={{
          fontFamily: '"Playfair Display", serif',
          fontSize: '2rem',
          marginBottom: '0.5rem',
          textAlign: 'center'
        }}>
          Upload dit rum
        </h2>
        
        <p style={{
          color: '#666',
          textAlign: 'center',
          marginBottom: '2rem'
        }}>
          Tag et foto eller upload et eksisterende billede
        </p>
        
        {/* Drag & drop zone */}
        <div 
          onDragOver={(e) => { e.preventDefault(); setIsDragging(true); }}
          onDragLeave={() => setIsDragging(false)}
          onDrop={(e) => { e.preventDefault(); /* håndter upload */ }}
          style={{
            border: `3px dashed ${isDragging ? '#000' : '#ddd'}`,
            borderRadius: '16px',
            padding: '4rem 2rem',
            textAlign: 'center',
            background: isDragging ? '#f9f9f9' : 'transparent',
            transition: 'all 0.3s ease',
            cursor: 'pointer'
          }}
        >
          <div style={{
            width: '80px',
            height: '80px',
            margin: '0 auto 1rem',
            background: '#f5f5f5',
            borderRadius: '50%',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center'
          }}>
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
              <polyline points="17,8 12,3 7,8"/>
              <line x1="12" y1="3" x2="12" y2="15"/>
            </svg>
          </div>
          
          <p style={{ fontWeight: 500, marginBottom: '0.5rem' }}>
            Træk og slip dit billede her
          </p>
          <p style={{ color: '#999', fontSize: '0.9rem' }}>
            eller klik for at vælge fil
          </p>
        </div>
        
        {/* Eksempel billeder */}
        <div style={{ marginTop: '2rem' }}>
          <p style={{ color: '#666', fontSize: '0.9rem', marginBottom: '1rem' }}>
            Eller prøv et eksempel:
          </p>
          <div style={{ display: 'flex', gap: '1rem', justifyContent: 'center' }}>
            {['Stue', 'Soveværelse', 'Køkken'].map(room => (
              <button key={room} style={{
                padding: '0.75rem 1.5rem',
                border: '1px solid #ddd',
                borderRadius: '8px',
                background: 'white',
                cursor: 'pointer',
                fontSize: '0.9rem'
              }}>
                {room}
              </button>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
3. Før/efter showcase (som Collov's slider)
tsx
Copy
// /src/components/BeforeAfterShowcase.tsx

export function BeforeAfterShowcase() {
  const [sliderPosition, setSliderPosition] = useState(50);
  
  return (
    <div style={{
      padding: '6rem 2rem',
      background: '#1a1a1a',
      color: 'white'
    }}>
      <h2 style={{
        fontFamily: '"Playfair Display", serif',
        fontSize: '2.5rem',
        textAlign: 'center',
        marginBottom: '3rem'
      }}>
        Fra drøm til virkelighed
      </h2>
      
      {/* Before/After slider */}
      <div style={{
        maxWidth: '1000px',
        margin: '0 auto',
        position: 'relative',
        borderRadius: '16px',
        overflow: 'hidden'
      }}>
        <div style={{ position: 'relative', aspectRatio: '16/9' }}>
          {/* After (baggrund) */}
          <img 
            src="/after-room.jpg" 
            alt="Efter"
            style={{ width: '100%', height: '100%', objectFit: 'cover' }}
          />
          
          {/* Before (foreground med clip) */}
          <div style={{
            position: 'absolute',
            top: 0,
            left: 0,
            width: '100%',
            height: '100%',
            clipPath: `inset(0 ${100 - sliderPosition}% 0 0)`
          }}>
            <img 
              src="/before-room.jpg" 
              alt="Før"
              style={{ width: '100%', height: '100%', objectFit: 'cover' }}
            />
          </div>
          
          {/* Slider handle */}
          <div 
            onMouseDown={(e) => /* drag logic */ }
            style={{
              position: 'absolute',
              top: 0,
              bottom: 0,
              left: `${sliderPosition}%`,
              width: '4px',
              background: 'white',
              cursor: 'ew-resize',
              transform: 'translateX(-50%)'
            }}
          >
            <div style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              width: '48px',
              height: '48px',
              background: 'white',
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 4px 12px rgba(0,0,0,0.3)'
            }}>
              ↔
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}