OPTIMERET SHOPPABLE AI IMAGE — Nordic Homebuild AI

Du har allerede bygget dette (ændr IKKE):
- server/yolo.ts — YOLO detection
- server/cropImage.ts — image cropping  
- server/huggingFace.ts — CLIP vector generation
- server/vectorSearch.ts — pgvector database search
- POST /api/analyze-image — finder møbler
- POST /api/find-similar-crop — finder lignende produkter
- FurnitureDetector — frontend komponent

OPGAVER NU (ændr eksisterende kode):

1. SUBTILE SHOPPING INDICATORS (erstat farvede kasser)
   FJERN: farvede rektangler/bokse over møbler
   TILFØJ: 
   - Lille hvid cirkel, 10px, border: 1px solid rgba(0,0,0,0.2)
   - Centeret på møblet (x + width/2, y + height/2)
   - Ikon: indkøbsvogn (ShoppingCart fra lucide-react)
   - Kun synlig når brugeren hover/trykker på AI-billedet
   - Animation: fadeIn 0.3s, pulse 2s infinite
   - z-index: 10

2. HOVER EFFEKT (erstat eksisterende)
   FJERN: farvet highlight zone
   TILFØJ:
   - Cursor: pointer på hele AI-billedet
   - Tooltip ved hover på prik: "{labelDa}" (Sofa, Bord, Lampe)
   - Tooltip style: bg-black/80, text-white, px-2 py-1, rounded, text-xs
   - Ingen farvet zone — kun prikken og tooltip

3. KLIK → PRODUKTPANEL (ændr eksisterende popup)
   STRUKTUR:
   ┌─────────────────────────────────────────┐
   │  ✕                                      │
   │  BEST MATCH                             │
   │  ┌─────────────────┐                    │
   │  │                 │  Navn             │
   │  │   Stort billede │  Pris             │
   │  │                 │  Shop             │
   │  │                 │  [Shop nu →]      │
   │  └─────────────────┘                    │
   ├─────────────────────────────────────────┤
   │  SIMILAR        │  BUDGET             │
   │  ┌─────────┐    │  ┌─────────┐       │
   │  │ Lille   │    │  │ Lille   │       │
   │  │ billede │    │  │ billede │       │
   │  └─────────┘    │  └─────────┘       │
   │  Navn           │  Navn               │
   │  Pris           │  Pris               │
   │  [Se →]         │  [Se →]             │
   │  "Ligner mest"  │  "Budget valg"      │
   └─────────────────────────────────────────┘

   DATA:
   - BEST MATCH: results[0] (højest similarity)
   - SIMILAR: results[1] og results[2]
   - BUDGET: laveste pris fra results[0-4], fremhævet med grøn badge "Budget"

   STYLING:
   - bg-white, rounded-xl, shadow-2xl, max-w-2xl
   - Padding: 24px
   - Gap mellem sektioner: 16px
   - "Shop nu" knap: bg-black text-white px-4 py-2 rounded
   - "Budget" badge: bg-green-100 text-green-800 px-2 py-0.5 rounded text-xs

4. ONBOARDING (nyt — tilføj til home.tsx)
   UNDER AI-billedet, før FurnitureDetector knap:
   - Tekst: "💡 Klik på de hvide prikker for at shoppe lignende møbler"
   - Style: text-sm text-gray-500 text-center mt-2
   - Fades ud efter 5 sekunder (eller når brugeren klikker første gang)
   - Gem i localStorage: "hasSeenOnboarding": true

5. MOBIL TILPASNING (ændr eksisterende)
   - Touch: vis prikker automatisk (ikke kun ved hover)
   - Tap på prik = åbn panel
   - Swipe på panel = luk
   - Panel: full-screen på mobil, ikke modal

IMPLEMENTATION:
- Rediger eksisterende FurnitureDetector.tsx (ikke lav ny fil)
- Brug eksisterende API endpoints (ikke nye)
- Test: analyze-image skal returnere samme format som før

BYG DETTE NU. Vis ikke kode. Vis ikke forklaringer. Bare implementer.